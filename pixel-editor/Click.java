/**
 * The Click enum is used to determine which button was clicked by the user
 * So the tools can have multiple functions based on button click
 */
public enum Click {
    LEFT,
    RIGHT,
    MIDDLE
}
