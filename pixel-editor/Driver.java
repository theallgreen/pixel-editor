public class Driver
{
    /**Main creates an initial display to customize the easel*/
    public static void main(String args[]) //trying to fix stuff
    {
        ResolutionScreen r = new ResolutionScreen();
        // new Easel(r.returnHeight(), r.returnWidth());
    }
}
